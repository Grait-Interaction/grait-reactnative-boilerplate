import axios from 'axios'

export const GET_<%= props.name.toUpperCase() %> = 'GET_<%= props.name.toUpperCase() %>'

export const get<%= props.name %> = () => ({
    type: GET_<%= props.name.toUpperCase() %>,
    payload: axios.get('<%= props.name.toLowerCase() %>/')
})